import Navbar from './Navbar';

function Header () {
    return(
        <header className="header">
            <h1 className="head">Art Club</h1>
            <Navbar></Navbar>
        </header>
    )
}

export default Header;