import Navbar from './Navbar';

function Header () {
    return(
        <header>
            <h1 className="header">Art Club</h1>
            <Navbar></Navbar>
        </header>
    )
};

export default Header;