import Navbar from './Navbar';

function Header () {
    return(
        <header>
            <h1 className="header rainbow-text">Art Club</h1>
            <Navbar></Navbar>
        </header>
        
    )
};

export default Header;